<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="org.martinrgb.hiframeanimation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="org.martinrgb.hiframeanimation.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name="org.martinrgb.hiframeanimation.GIFLiveWallpaper"
            android:label="MultiLWP(GIFF)"
            android:permission="android.permission.BIND_WALLPAPER"
            android:process=":wallpaper">
            <!-- 为实时壁纸配置intent-filter -->
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
            </intent-filter>
            <!-- 为实时壁纸配置meta-data -->
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@mipmap/ic_launcher" />
        </service>


        <!--<service-->
            <!--android:name="org.martinrgb.hiframeanimation.ShaderWallpaper"-->
            <!--android:label="MultiLWP(Framee)"-->
            <!--android:permission="android.permission.BIND_WALLPAPER"-->
            <!--android:process=":wallpaper">-->
            <!--&lt;!&ndash; 为实时壁纸配置intent-filter &ndash;&gt;-->
            <!--<intent-filter>-->
                <!--<action android:name="android.service.wallpaper.WallpaperService" />-->
            <!--</intent-filter>-->
            <!--&lt;!&ndash; 为实时壁纸配置meta-data &ndash;&gt;-->
            <!--<meta-data-->
                <!--android:name="android.service.wallpaper"-->
                <!--android:resource="@mipmap/ic_launcher" />-->
        <!--</service>-->

    </application>

    <uses-permission android:name="android.permission.SET_WALLPAPER"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>


</manifest>